# B5 Verify — Security Center & trust

Verified live in the running Open Cowork Electron window (dark theme, Chinese locale). Screenshots in `/tmp/cowork-study/b5-*.png`.

| #   | Point                       | Result          | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Security Center tab         | **PASS**        | Settings shows 安全中心 (Security) tab with ShieldCheck icon, subtitle "沙盒、删除保护、防护与授权，统一管理". Old standalone Sandbox tab gone. Contains: deletion-protection toggle (default ON), embedded Sandbox isolation section (real enable button + VM status "沙盒已禁用 - 命令直接在系统上运行"), informational 路径隔离 / 危险命令拦截 rows badged 始终开启 (always-on) and 高风险工具先询问 approval row badged always-on. b5-02/03.                                                                                                                        |
| 1b  | Toggle persists             | **PASS**        | Toggled deletion protection OFF (b5-05), switched to API tab and back — still OFF (b5-06). Restored to ON (b5-07); still ON after deep-link (b5-11).                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1c  | Copy accuracy               | **PASS**        | Copy is plain-language and accurate. Explicit gap note: "此功能会在删除执行前请求确认，但不会将文件移入回收站，也不会设置按文件数量的批量删除上限——这些行为后端并未强制执行。" (asks before delete; does NOT move to Trash, does NOT set a file-count bulk limit). No fake "protects" claims. Guards/approval rows honestly labeled informational always-on.                                                                                                                                                                                                            |
| 2   | Skills capability badges    | **PASS**        | Builtin skill cards show 仅本地 (Local-only) badge + lock icon (b5-09). Source: `getSkillCapabilityBadges` returns local-only for builtin/custom, connector for mcp.                                                                                                                                                                                                                                                                                                                                                                                                    |
| 3   | Connector capability badges | **PASS**        | Presets show 本地进程 (Local process, terminal icon) for stdio; 使用凭证 (Uses credentials, key icon) on Notion & Software_Development; Notion also 需要令牌 (b5-10). Network badge exists in source (`getConnectorCapabilityBadges`) for sse/streamable-http; no network preset present to render it.                                                                                                                                                                                                                                                                  |
| 4   | Dark theme                  | **PASS**        | All screenshots captured in dark theme.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | Legacy `sandbox` deep-link  | **PASS (live)** | `SettingsPanel.normalizeTabId`: `raw === 'sandbox'` → `'security'`. Verified live: `GET /navigate?page=settings&tab=sandbox` (nav-server) landed on 安全中心 (b5-11).                                                                                                                                                                                                                                                                                                                                                                                                   |
| 6   | Enforcement source + tests  | **SOURCE-ONLY** | `permission-rules-store.ts` `decidePermission` downgrades an auto-allowed destructive delete to `ask` only when `deletionProtectionEnabled` and never weakens a `deny` (base==='allow' guard, lines 143-163). `PermissionDialog.tsx` renders destructive (Trash2/error) + bulk-delete warnings via `isDestructiveDeleteRequest`/`isBulkDeleteRequest`. Tests exist and PASS: `src/tests/config/permission-rules-store.test.ts` + `src/tests/utils/destructive-command.test.ts` (42 tests green). Cannot exercise live without a real delete tool-call (no credentials). |

## Notes

- No inaccurate/misleading security copy found — the deletion-protection gap note is honest and specific.
- No regressions observed. Legacy sandbox entry cleanly maps to Security.

## Cleanup

- Deletion-protection restored to ON (its prior/default state) — confirmed in b5-07 and b5-11.
- No ollama set added, no config files edited.
- Dev server killed; no lingering electron/vite processes.

VERDICT: **B5 VERIFY: PASS**
